# ver26 — filter + stock header UX

- Fixed static `inventory.html` filter by listening to both `input` and `change` events and explicitly toggling card display.
- Kept the React/Next filter state synchronized with max price.
- Reduced the stock page header height.
- Applied the same `hero-lounge.svg` background used on the homepage to the stock page hero.
- Reduced the main heading size and shortened the SEO text for the stock catalog.
- Kept Montserrat from ver25 and preserved routes, JSON-LD, sitemap and semantics.
